# Aneumo compact-cache exact-path inventory

**Decision date:** 2026-08-13

**Evidence layer:** user-supplied exact introai9 inventory report plus bounded
current-session connection provenance

**Scientific state:** cache identity resolved; P0 v3 remains 0/12 and locked

## What is now resolved

A bounded private inventory resolves the exact location of the previously
verified Aneumo compact holding. The location, byte count, sibling filenames
and infrastructure details are intentionally withheld from this public
repository. The report records SHA-256
`9640b0efbc8ff17a8382b1592547bef109620faeced8a004a932b3cde3b97ab9`.
That digest exactly matches the immutable cache identity used by the historical
staging, scaling and current response-fidelity contracts. Adjacent provenance
files were reported present. HDF5 schema was checked, but array values were not
read. No second copy was reported in the bounded private scope.

This corrects only the asset-location layer. It does not create a train split,
endpoint value, performance result, model or paper claim. The verified holding
contract remains 32 generation families, 64 cases, eight flows and 4,096
aligned nodes; the method-free P0 uses only its historical 20 training
families.

## Why execution is still locked

The immutable P0 v3 config remains the pre-inventory, non-executable scientific
contract and is not edited in place. A separate activation manifest has not
been registered. In this session, one WSL SSH attempt ended before the network
because the Windows alias was unavailable; one Windows credential-managed
attempt reached the endpoint but was reset before any remote command ran.
Therefore current container readability, scheduler state and operational
execution readiness have no new verdict.

No HDF5 array, scientific endpoint, queue state or GPU was read. No transfer or
PBS submission occurred. `junjinyong` was never accessed.

## Next admissible action

After a newly verified introai9 operational change, check only the already
pinned container path. If readable, register a separate activation manifest
that references the immutable P0 v3 config and freezes the private cache
identity, checksum, container, public commit, CPU 4/GPU 0 PBS envelope and
one-shot output root. Do not edit v3, broaden the inventory, read validation or
test fields, or create a GPU job. A scientific 12/12 pass may authorize only a
fresh P1 evidence version.
