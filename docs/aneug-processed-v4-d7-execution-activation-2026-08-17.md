# D7 train-field admission execution activation

The user explicitly selected D7 on 17 August 2026. This creates a fresh
one-shot execution contract; it does not modify the dormant D7 draft and does
not repair, resume, rerun, reopen or relabel closed D6. Closed E0 remains
infrastructure classification rather than scientific evidence.

D7 binds the byte-exact D6 method-free evaluator, thresholds and D5 component
split. It may read only 406 D5-train tensors and the shared finest faces.
Validation, outer-test and auxiliary tensor values remain sealed. It fits no
model, selects no architecture, uses no GPU and produces no paper result.

The wrapper incorporates E0's verified execution envelope. The private record
directory exists before submission; the attempt marker and internal log are
created before strict shell mode or any environment/scientific check. It never
sources `/etc/profile` and does not treat scheduler stdout/stderr as evidence.

After public Quality and private activation, exactly one introai9 PBS attempt
is permitted at CPU 4, 64 GB and GPU 0. Pass, scientific fail or execution
incomplete closes D7 without same-contract repair or resubmission. Only a pass
permits registration of a fresh bounded train/validation baseline-development
version. Outer access, immediate GPU training and manuscript claims remain
unauthorized. The public static site is not maintained. Never use junjinyong.
