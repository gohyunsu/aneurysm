# AneuG processed-v4 D5 outcome

The sole D5 attempt ran as `introai9` PBS job `116483.ECE-util1` with CPU 4,
64 GB and GPU 0. It reached final state F with exit 0 after 00:01:19. D5 is
closed 1/1 and may never be rerun or repaired.

The exact source identity and D4 case order were reverified. All 578 aligned
GHD rows are finite float32 vectors of length 432. They form 576 connected
components: two exact duplicate components, no additional fixed-tolerance
numerical edge and maximum component size two. The 508 primary `stable_*`
cases form 508 distinct components. The 70 filename-defined auxiliary cases
form 68 components; no component mixes primary and auxiliary cases.

The prospective gate therefore passes. The private synthetic-geometry-
component split is frozen at 406 train, 51 validation and 51 outer-test
components, with all 80 phases following the component. Case IDs and component
membership remain private. This unit is not a patient, site or authoritative
generator family.

D5 read no registered field tensor, WSS, coordinate, normal, connectivity or
external geometry payload and computed no scientific field endpoint. Its
scientific verdict remains JSON `null`. The pass permits only registration of
a later field audit and bounded development; it does not execute or authorize
an architecture, GPU training, validation/test access, result or paper claim.
The public static site was not changed and is not an activation gate.
