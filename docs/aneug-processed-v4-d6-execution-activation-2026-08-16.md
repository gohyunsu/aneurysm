# AneuG processed-v4 D6 execution activation

**Activated · 2026-08-16:** the user explicitly selected D6. This is a fresh
executable version bound to the immutable non-executable registration; it does
not edit or relabel that registration. The only authorized scientific read is
the 406-case D5 training split.

## One question, one attempt

D6 asks whether the acquired transient target is internally usable before any
architecture is chosen. The one CPU-only PBS attempt recomputes both processed
file hashes, verifies the exact private D5 manifest and then applies the
already-registered method-free gate. It reads no validation, outer-test or
auxiliary tensor value and fits no model.

The envelope is introai9, PBS coss_agpu, CPU 4, memory 64 GB, GPU 0 and three
hours. An attempt marker is created before either processed object is loaded.
Completion, scientific failure or execution failure closes D6; no same-contract
repair, resume or resubmission exists. junjinyong is excluded.

## Outputs and privacy

The public result contains only aggregate gate diagnostics. It cannot contain
case identifiers, physical train normalization values or absolute paths. The
train-only physical statistics needed by a later baseline are written to a
separate private object. Both JSON files are strict, atomic and refuse
overwrite. Raw scheduler and execution logs remain private.

## Consequence boundary

A pass permits only a new, bounded train/validation baseline-development
registration. It does not authorize outer-test access, GPU training, a selected
architecture or a paper claim. Failure identifies a target-level obstruction;
execution-incomplete records no scientific verdict. The public static site is
outside this code-and-experiment workflow.
