# D6 execution v1 preflight withdrawal and v2 activation

The Quality-passed v1 activation was stopped before field access and before PBS
submission. A no-field introai9 preflight found that its registered transient
path and private D5 manifest were exact, while its steady normalization path
did not exist. The exact steady object is the already preserved D2 temporary
object used by D3 and D4. No output record was created and D6 attempts remain
0/1.

V1 is now fail-closed and cannot execute. Fresh v2 changes only the steady
server-relative path and its own output/contract version. The steady byte size,
SHA-256, immutable D6 gate, D5-train IDs, thresholds, CPU 4 / 64 GB / GPU 0
resources and one-attempt/no-repair rule are unchanged.

V2 still reads only the 406 D5-train tensors. Validation, outer-test and
auxiliary values remain sealed; model fitting, architecture selection, GPU
training and paper claims remain unauthorized. The public static site is not
maintained. Never access, query, transfer to, submit on or monitor junjinyong.
