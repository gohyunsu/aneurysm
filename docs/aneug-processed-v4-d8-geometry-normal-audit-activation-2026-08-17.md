# D8 mesh-normal identifiability audit activation

The user explicitly selected D8 on 17 August 2026. D8 is a fresh geometry-
normal identifiability question, not a change to, reinterpretation of or rerun
of any closed D7 threshold or outcome.

D8 reads only the 406 D5-train tensors and shared finest faces. It records a
descriptive stored-normal magnitude census, but those cutoffs neither select
nor modify the gate. The scientific endpoint is whether coordinates and faces
deterministically yield sufficiently covered unit normals whose direction and
WSS tangency satisfy frozen limits. Geometry repair is preprocessing hygiene,
not novelty or paper evidence.

The prospective one-shot envelope is introai9 PBS, CPU 4, 64 GB and GPU 0.
The private attempt marker and log precede strict mode, `/etc/profile` is never
sourced inside the wrapper, and scheduler stdout/stderr is not evidence. All
numeric results remain private. Validation, outer-test and auxiliary fields,
models, GPU training and paper claims remain forbidden.

Any pass, scientific fail or execution-incomplete outcome closes D8 without
repair or resubmission. A pass permits only registration of a fresh mesh-
canonicalized train/validation development version. It does not immediately
authorize training or outer access. The public site is not maintained. Never
use junjinyong.
