# Aneumo family-mapping authority watch v22

**Date:** 2026-08-14  
**Status:** watch-only · all 35 frozen public states match · no scientific authority

## Outcome

The restored `introai9` service removes an infrastructure obstacle, but it does
not resolve the two source-contract obstacles that caused stability P0 v1 to be
withdrawn at field access 0/60. The current Aneumo family table is still not an
authoritatively completed review, and the Hugging Face and GitHub licence
statements still conflict. Therefore no successor P0, model training or GPU
execution is activated.

Source-watch v22 adds a fail-closed monitor for
[official Aneumo issue 4](https://github.com/Xigui-Li/Aneumo/issues/4). It is
independent of the existing watches for the official repository head and the
Hugging Face dataset revision/licence tag.

## Exact frozen issue state

- issue state: closed
- issue update: `2026-07-24T13:38:27Z`
- comments: 6
- owner comments: `3236749321`, `5070184242`, `5070473308`
- owner error acknowledgement: `5070184242`
- owner complete-review commitment: `5070473308`

The contract pins every comment ID, update timestamp and body SHA-256. A live
read-only refresh on 2026-08-14 matched the frozen snapshot exactly. This means
there is no new authority-bearing correction; it does not mean that the current
mapping has become authoritative.

## What a detected change means

Any issue-state, comment-set, owner-comment, body or timestamp change can emit
only `fresh_source_reaudit_only`. The monitor cannot:

- interpret an issue comment as a corrected mapping release;
- interpret a repository-head change as proof that the promised full review is
  complete;
- interpret a licence-tag change as a legal resolution;
- reactivate the withdrawn 12-family P0 panel;
- download fields, register P0, select a method, train a model or authorize GPU
  or outer-test execution.

A human source audit must instead establish all of the following: an explicit
authoritative corrected mapping, exact version/hash, coverage of the relevant
transient release, and a separately resolved licence statement. Only then may a
new evidence version perform a fresh field-blind family selection. The old
panel remains immutable history even if the source is corrected later.

## Research consequence

The scientific direction remains **transient structure-faithful WSS at
30.0/40 inactive**. D0 v2 established bounded reader/extractor operability on
two previously inspected members, not critical-structure stability. The next
scientific step is still a new method-free stability P0; it is blocked by
source authority rather than by server health. Architecture, GNN selection,
GPU training, paper contribution and result claims remain zero.

Execution, if later authorized by a fresh contract, is `introai9` PBS-only with
no login-node GPU. `junjinyong` is excluded from access, query, transfer,
submission and monitoring.
