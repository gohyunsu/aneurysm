# Release-730 response-oracle execution relocation

The introai9 host7 oracle remains queued because all four requested-host GPUs
are assigned. On 2026-08-24 the user explicitly authorized a conditional
fallback through the `junjinyong` account. This record permits one execution
relocation; it does not change the scientific experiment.

The original response-oracle configuration, implementation and activation are
hash-bound at public commit `1c5039292d60e9dcbb722318e43bacc66de1ad36`.
The 584/73 development split, sealed 73-case test, sealed 79 extras, train-only
basis, true-validation oracle coefficients, rank grid and every metric remain
unchanged. The relocation changes only the PBS account, queue, lack of a host
constraint, owner-readable staged paths and output root.

Execution requires a fresh private activation that binds the Quality-passed
relocation commit and the exact base activation. The staged transient and
steady objects, private split and private train audit must reproduce their
registered hashes before any field is loaded. The result remains a
development-only representation ceiling, not learned performance, rank
selection or a manuscript performance claim.
